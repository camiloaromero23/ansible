- name: Cloning password-store
  ansible.builtin.git:
    repo: "git@github.com:camiloaromero23/.password-store.git"
    dest: "{{ lookup('env', 'HOME') }}/.password-store"
    version: main
  tags:
    - install
    - dotfiles
    - stow
